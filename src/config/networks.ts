export const env = {
  SERVICES: {
    sequenceApi: 'https://api.sequence.app',
    metadata: 'https://metadata.sequence.app',
    indexer: 'https://${network.name}-indexer.sequence.app',
    indexerGateway: 'https://indexer.sequence.app',
    imageProxy: 'https://imgproxy.sequence.xyz/'
  }
}
